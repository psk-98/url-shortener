# URL Shortener

This part of the project for just messsing around with[nextjs](https://nextjs.org/) which i'm already familar with, so i might change to [tan stack](https://tanstack.com/) just learn that

# Development

```bash
cp exapmle.env .env
bun install
bun --bun dev
# you can use your prefered package manager npm ,pnpm etc..
```

Visit [http://localhost:3000](http://localhost:3000)
